# Development dependencies are declared in pyproject.toml, under the [dev]
# extra. This file installs them, and is kept so that existing setups that
# reference it keep working.
#
#     pip install -r requirements_dev.txt
#
# is equivalent to
#
#     pip install -e ".[dev]"
-e .[dev]
