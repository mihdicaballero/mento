# Documentation dependencies are declared in pyproject.toml, under the [docs]
# extra. Read the Docs installs that extra directly (see .readthedocs.yaml);
# this file is kept for local builds.
#
#     pip install -r docs/requirements_docs.txt
#
# is equivalent to
#
#     pip install -e ".[docs]"
-e ..[docs]
